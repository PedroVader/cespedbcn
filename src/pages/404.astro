---
// src/pages/404.astro
import Layout from '../layouts/Layout.astro';

const pageTitle = 'Página no encontrada | CespedBCN';
---

<Layout title={pageTitle}>

  <main class="min-h-[70vh] flex items-center justify-center px-4 py-16">
    <div class="text-center max-w-lg">
      
      <!-- Número 404 con estilo césped -->
      <div class="relative mb-8">
        <span class="text-[150px] md:text-[200px] font-black text-green-100 leading-none select-none">
          404
        </span>
        <div class="absolute inset-0 flex items-center justify-center">
          <span class="text-6xl md:text-8xl font-black text-green-600">
            404
          </span>
        </div>
      </div>

      <!-- Mensaje -->
      <h1 class="text-2xl md:text-3xl font-bold text-slate-900 mb-4">
        ¡Vaya! Esta página no existe
      </h1>
      <p class="text-slate-600 mb-8">
        Parece que el césped no ha crecido aquí todavía. 
        La página que buscas no existe o ha sido movida.
      </p>

      <!-- Acciones -->
      <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
        <a 
          href="/" 
          class="inline-flex items-center gap-2 bg-green-600 text-white px-6 py-3 rounded-xl font-bold hover:bg-green-700 transition"
        >
          <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" d="m2.25 12 8.954-8.955c.44-.439 1.152-.439 1.591 0L21.75 12M4.5 9.75v10.125c0 .621.504 1.125 1.125 1.125H9.75v-4.875c0-.621.504-1.125 1.125-1.125h2.25c.621 0 1.125.504 1.125 1.125V21h4.125c.621 0 1.125-.504 1.125-1.125V9.75M8.25 21h8.25" />
          </svg>
          Volver al inicio
        </a>
        <a 
          href="/productos" 
          class="inline-flex items-center gap-2 bg-white text-slate-700 px-6 py-3 rounded-xl font-bold border border-gray-200 hover:border-green-300 hover:text-green-600 transition"
        >
          Ver productos
          <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5 21 12m0 0-7.5 7.5M21 12H3" />
          </svg>
        </a>
      </div>

      <!-- Links útiles -->
      <div class="mt-12 pt-8 border-t border-gray-200">
        <p class="text-sm text-slate-500 mb-4">También puedes visitar:</p>
        <div class="flex flex-wrap justify-center gap-4 text-sm">
          <a href="/productos" class="text-green-600 hover:text-green-700 font-medium">Catálogo</a>
          <span class="text-slate-300">·</span>
          <a href="/servicios" class="text-green-600 hover:text-green-700 font-medium">Servicios</a>
          <span class="text-slate-300">·</span>
          <a href="/contacto" class="text-green-600 hover:text-green-700 font-medium">Contacto</a>
        </div>
      </div>

      <!-- Contacto directo -->
      <div class="mt-8 bg-slate-50 rounded-xl p-4">
        <p class="text-sm text-slate-600">
          ¿Necesitas ayuda? Llámanos al 
          <a href="tel:934850085" class="font-bold text-green-600 hover:text-green-700">934 850 085</a>
        </p>
      </div>

    </div>
  </main>

</Layout>